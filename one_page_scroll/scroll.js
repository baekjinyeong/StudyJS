(function(){
	'use strict';
	console.log('test');

	/*
		1. 각 div태그의 top 값을 구한다.
		2. page 클릭 이벤트를 형성한다.
		3. 버튼 클릭 시 해당 버튼의 인덱스 값을 부여해준다.
		4. 클릭 버튼의 인덱스 값에 div 태그의 top 값을 곱해준다. (animate 사용)
	*/

	var scrolling = function(){
		
		var Obj = {
			pageBtn : '.btn-slide-page li a',
		};

	};
	scrolling();
}());
;
